const container = {
    marginTop: 16
};

const paper = {
    width: "100%",
    overflowX: "auto"
};

const inner_container = {
    marginTop: 32,
    marginBottom: 32
};

export const styles = {
    container: container,
    paper: paper,
    inner_container: inner_container
};
